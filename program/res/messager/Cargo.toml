[package]
name = "messager"
version = "0.1.0"
edition = "2021"

[dependencies]
gstd = { path = "../../../gstd" }

[build-dependencies]
gear-wasm-builder = { path = "../../../utils/wasm-builder" }

[features]
std = []
default = ["std"]
