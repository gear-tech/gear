[package]
name = "messager"
version = "0.1.0"
edition = "2021"

[dependencies]
gstd = { git = "https://github.com/gear-tech/gear" }

[build-dependencies]
gear-wasm-builder = { git = "https://github.com/gear-tech/gear" }

[features]
std = []
default = ["std"]
