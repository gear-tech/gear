name: Crates IO

on:
  workflow_dispatch:
    inputs:
      publish:
        description: "If publish packages"
        type: boolean
        default: false
      version:
        description: "Workspace version to publish"
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: short
  TERM: xterm-256color

jobs:
  check:
    runs-on: [kuberunner]
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - name: "ACTIONS: Checkout"
        uses: actions/checkout@v4

      - name: "Install: Rust nightly-2024-12-14 toolchain"
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2024-12-14
          targets: wasm32v1-none
          components: llvm-tools

      - name: "Publish packages (simulate)"
        if: ${{ !inputs.publish }}
        run: cargo +nightly-2024-12-14 run --release -p crates-io publish -v ${{ inputs.version }} --simulate --registry-path /tmp/cargo-http-registry

      - name: "Publish packages"
        if: ${{ inputs.publish }}
        run: cargo +nightly-2024-12-14 run --release -p crates-io publish -v ${{ inputs.version }}
