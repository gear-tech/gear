name: gstd-CI

on:
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    if: contains(github.event.pull_request.labels.*.name, 'D3-gstd')
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Check fmt
      run: |
        cargo fmt --package gstd -- --check

    - name: Run clippy
      run: |
        cargo clippy --package gstd --all-features -- -D warnings

  build:
    if: contains(github.event.pull_request.labels.*.name, 'gstd')
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build
      run: |
        cargo build --release --package gstd --all-features

    - name: Run tests
      run: |
        cargo test --release --package gstd --all-features

  test:
    if: contains(github.event.pull_request.labels.*.name, 'gstd')
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install toolchains
      uses: actions-rs/toolchain@v1
      with:
        toolchain: nightly
        target: wasm32-unknown-unknown

    - name: Test examples
      run: ./scripts/test.sh
