[package]
name = "tests-btree"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2018"
license = "GPL-3.0"

[dependencies]
gstd = { path = "../../gstd", features = ["debug"] }
codec = { package = "parity-scale-codec", version = "2", default-features = false, features = ["derive"] }

[build-dependencies]
substrate-wasm-builder = { version = "5.0.0-dev", git = "https://github.com/gear-tech/substrate.git", branch = "gear-latest" }

[dev-dependencies]
common = { package = "tests-common", path = "../common" }

[lib]

[features]
std = ["codec/std"]
default = ["std"]
