[package]
name = "demo-async-init"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
gstd = { workspace = true }
futures = { version = "0.3", default-features = false, features = ["alloc"] }
