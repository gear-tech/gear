[package]
name = "program-factory"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
gcore = { path = "../../gcore", features = ["debug"] }
gstd = { path = "../../gstd", features = ["debug"] }
hex-literal = "0.3.4"