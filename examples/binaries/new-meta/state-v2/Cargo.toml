[package]
name = "demo-meta-state-v2"
version = "0.1.0"
edition = "2021"

[dependencies]
gstd.workspace = true
gmeta = { path = "../../../../gmeta", features = ["codegen"] }
demo-meta-io = { path = "../io" }

[build-dependencies]
gear-wasm-builder = { path = "../../../../utils/wasm-builder", features = ["metawasm"] }

[features]
default = ["std"]
std = []
