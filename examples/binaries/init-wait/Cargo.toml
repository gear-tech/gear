[package]
name = "demo-init-wait"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2021"
license = "GPL-3.0"
workspace = "../../../"

[dependencies]
gstd = { workspace = true, features = ["debug"] }

[build-dependencies]
gear-wasm-builder.workspace = true

[lib]

[features]
std = []
default = ["std"]
