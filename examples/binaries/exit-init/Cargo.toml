[package]
name = "demo-exit-init"
version = "0.1.0"
authors = { workspace = true }
edition = "2021"
license = "GPL-3.0"
workspace = "../../../"

[dependencies]
gcore = { workspace = true }
gstd = { workspace = true }

[build-dependencies]
gear-wasm-builder = { workspace = true }

[lib]

[features]
debug = ["gstd/debug"]
std = []
default = ["std"]
