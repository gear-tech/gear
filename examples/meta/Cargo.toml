[package]
name = "demo-meta"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2018"
license = "GPL-3.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
gstd = { path = "../../gstd", features = ["debug"] }
gstd-meta = { path = "../../gstd-meta" }
scale-info = { version = "0.12.0", default-features = false, features = ["derive"] }
