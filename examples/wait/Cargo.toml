[package]
name = "demo-wait"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2021"
license = "GPL-3.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
gcore = { workspace = true, features = ["debug"] }
gstd = { workspace = true, features = ["debug"] }
