[package]
name = "demo-incomplete-async-payloads"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
gstd = { workspace = true, features = ["debug"] }
