version: '3'
services:
  gear:
    build:
      context: .
    ports:
      - "127.0.0.1:30333:30333/tcp"
      - "127.0.0.1:9933:9933/tcp"
    image: chevdor/gear:latest
    volumes:
      - "gear-data:/data"
    command: gear-node

volumes:
  gear-data:
