[package]
name = "gear-backend-wasmtime"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2018"
license = "GPL-3.0"

[dependencies]
gear-core = { path = "../../core" }
gear-backend-common = { path = "../common" }

wasmtime = { version = "0.34.1", default-features = false, features = ["parallel-compilation", "cranelift"] }
log = "0.4.1"
