[package]
name = "gear-backend-wasmtime"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2018"
license = "GPL-3.0"

[dependencies]
gear-core = { path = "../../core" }
gear-backend-common = { path = "../common" }

wasmtime = { version = "0.27.0", default-features = false, features = ["parallel-compilation"] }
