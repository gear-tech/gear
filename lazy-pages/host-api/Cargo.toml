[package]
name = "gear-lazy-pages-host-api"
version = "0.1.0"
edition = "2024"

[dependencies]
gear-core.workspace = true
gear-lazy-pages-common.workspace = true
gear-lazy-pages.workspace = true
parity-scale-codec.workspace = true
tracing.workspace = true

sp-runtime-interface = { workspace = true, optional = true }

[features]
default = []
pass-by = ["sp-runtime-interface"]
