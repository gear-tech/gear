[package]
name = "gear-baremetal-weights-patcher"
version = "0.1.0"
edition.workspace = true

[dependencies]
syn = { workspace = true, features = ["parsing", "full", "visit"] }
clap = { workspace = true, features = ["derive"] }
proc-macro2 = { workspace = true, features = ["span-locations"]}
