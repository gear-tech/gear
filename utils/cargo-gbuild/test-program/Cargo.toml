[package]
name = "gbuild-test-program"
version = "0.1.0"
edition = "2021"

[dependencies]
gstd = { path = "../../../gstd" }

[features]
debug = ["gstd/debug"]
default = []
std = []

[lib]
crate-type = ["cdylib"]

[workspace]
