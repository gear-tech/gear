[package]
name = "gbuild-test-foo"
version = "0.1.0"
edition = "2021"

[dependencies]
gstd = { path = "../../../../gstd" }

[dev-dependencies]
gtest = { path = "../../../../gtest" }

[features]
debug = ["gstd/debug"]
default = []
std = []

[lib]
crate-type = ["cdylib"]
