[package]
name = "test-program"
version = "0.1.0"
edition = "2024"

[dependencies]
gstd = { path = "../../../gstd", features = ["debug"] }

[dev-dependencies]
gtest = { path = "../../../gtest" }
gsdk = { path = "../../../gsdk" }

[build-dependencies]
gear-wasm-builder = { path = ".." }

# To exclude this project from the outer workspace
[workspace]

[features]
a = []
b = []
