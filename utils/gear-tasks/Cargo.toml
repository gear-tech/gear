[package]
name = "gear-tasks"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
sc-executor = { workspace = true, optional = true }
sc-executor-common = { workspace = true, optional = true }
sc-client-api = { workspace = true, optional = true }
sp-state-machine = { workspace = true, optional = true, features = ["std"] }
runtime-primitives = { workspace = true, optional = true, features = ["std"] }

gear-tasks-runtime-api.workspace = true

sp-runtime-interface.workspace = true
sp-externalities.workspace = true
sp-storage.workspace = true
sp-api.workspace = true
frame-support.workspace = true

parity-scale-codec.workspace = true
futures-executor = { version = "0.3.30", default-features = false, optional = true }
log.workspace = true

[dev-dependencies]
gear-tasks = { workspace = true, features = ["std"] }

sp-io = { workspace = true, features = ["std"] }
sp-state-machine = { workspace = true, features = ["std"] }
futures-executor = "0.3.30"
gear-node-testing.workspace = true
substrate-test-client.workspace = true

env_logger.workspace = true

[features]
default = ["std"]
std = [
    "sc-executor",
    "sc-executor-common",
    "sc-client-api",
    "sp-state-machine",
    "runtime-primitives",
    "sp-runtime-interface/std",
    "sp-externalities/std",
    "sp-api/std",
    "gear-tasks-runtime-api/std",
    "frame-support/std",
    "parity-scale-codec/std",
    "futures-executor",
]
