[package]
name = "wasm-info"
version = "0.1.0"
edition = "2021"

[dependencies]
hex = { workspace = true, features = ["std"]}
parity-wasm.workspace = true
clap = { workspace = true, features = ["derive"] }
