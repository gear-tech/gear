[package]
name = "runtime-fuzzer-fuzz"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
runtime-fuzzer = { path = ".." }
gear-utils = { path = "../../utils" }
env_logger = "0.10"
log = { version = "0.4.17" }
once_cell = "1.17.0"

[[bin]]
name = "main"
path = "fuzz_targets/main.rs"
test = false
doc = false
