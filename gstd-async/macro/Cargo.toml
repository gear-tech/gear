[package]
name = "gstd-async-macro"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2018"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0.75", features = ["full"] }
quote = "1.0.9"
