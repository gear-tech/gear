[package]
name = "gstd-async"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2018"

[dependencies]
gcore = { path = "../gcore" }
gstd = { path = "../gstd" }
gstd-async-macro = { path = "macro" }
futures = { version = "0.3", default-features = false, features = ["alloc"] }
