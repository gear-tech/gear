[package]
name = "gbuiltin-proxy"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Types and traits to interact with proxy builtin actor."
documentation = "https://docs.rs/gbuiltin-proxy"
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords = ["gear", "proxy", "builtin", "delegation", "no-std"]
categories = ["cryptography::cryptocurrencies", "encoding", "no-std"]

[dependencies]
parity-scale-codec = { workspace = true, features = ["derive"] }
scale-info = { workspace = true, features = ["derive"] }
gprimitives = { workspace = true, features = ["codec"] }
gear-workspace-hack.workspace = true
