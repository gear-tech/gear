// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.33;

/**
 * @dev Interface for callbacks that is available in every caller of Mirror contract.
 */
interface ICallbacks {
    /**
     * @dev If reply is received, this method will be called by Mirror smart contract,
     *      but only if `gear_core::rpc::ReplyInfo` contains `gear_core::message::ReplyCode` that has `ReplyCode::Error(_)` variant.
     * @param messageId Message ID of reply, generated by `MessageId::generate_reply(replied_to)` in Rust-side.
     * @param payload Payload of reply (e.g., error message in case of `UserspacePanic`).
     * @param replyCode Reply code of reply (`gear_core::message::ReplyCode.to_bytes()`).
     * @dev Currently this method has gas limit of 500_000 in order to prevent DoS attacks.
     *      Gas limit is proposed to be removed in the future when we switch to merkle roots approach for messages.
     */
    function onErrorReply(bytes32 messageId, bytes calldata payload, bytes4 replyCode) external payable;
}
